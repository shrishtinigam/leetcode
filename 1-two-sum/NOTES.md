Two Pointer approach implemented. 

### Time Complexity
O(N), as even in the worst case, we visit all the elements in the array only once.

### Space complexity
O(1). We use constant space for variables.